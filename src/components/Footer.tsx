import { footerText } from "@/data/site";

export function Footer() {
  return (
    <footer className="pt-9 text-center text-[10px] font-semibold uppercase tracking-[1.8px] text-[#aaa69f]">
      {footerText}
    </footer>
  );
}
